# Vero — variáveis de ambiente (EXEMPLO).
# Copie para .env e preencha. NUNCA commite o .env real (CLAUDE.md §4).
# Nenhum valor real/segredo deve existir neste arquivo.

# --- Ambiente ---
NODE_ENV=development

# --- Backend (apps/api) — preenchido a partir da S1/S2 ---
# DATABASE_URL=postgresql://user:password@localhost:5432/vero
# REDIS_URL=redis://localhost:6379
# JWT_SECRET=
# CORS_ORIGINS=http://localhost:3000
# PORT=3333
