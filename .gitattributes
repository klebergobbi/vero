# Normaliza fim de linha para LF no repositório (evita churn CRLF no Windows).
* text=auto eol=lf

# Binários — nunca normalizar.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary

# Lockfile — gerado, não diferenciar nem normalizar.
pnpm-lock.yaml -diff
