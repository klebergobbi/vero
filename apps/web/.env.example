# apps/web — variáveis de ambiente (EXEMPLO). Copie para apps/web/.env.local.
# Sem segredos no front: a URL da API é usada SERVER-SIDE (BFF), nunca exposta ao browser.

# Base do backend NestJS (apps/api). Em dev local, a API sobe na porta do seu .env da api.
API_BASE_URL=http://localhost:3333
