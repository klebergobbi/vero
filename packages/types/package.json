{
  "name": "@vero/types",
  "version": "0.0.0",
  "private": true,
  "description": "Tipos + schemas Zod compartilhados (catálogo de permissions, papéis de sistema, ações de auditoria). Fonte única reusada nos 4 apps (CLAUDE.md §2).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@vero/config": "workspace:*",
    "eslint": "^8.57.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
